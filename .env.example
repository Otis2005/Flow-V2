# Frontend (exposed to browser — anon key is OK to expose, RLS protects data)
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key

# Server-side only (never exposed) — used by Vercel serverless functions in /api
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...service-role-secret
ANTHROPIC_API_KEY=sk-ant-api03-...

# Allowed admin emails (comma-separated). The first one is the canonical admin.
ADMIN_EMAILS=kennedynange@gmail.com
